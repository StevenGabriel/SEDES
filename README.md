# SEDES

Practica Profesional 2026-1

* `sedes-frontend-1` está corriendo en **`http://localhost:5173/`** (`VITE v8.2.2 ready`).
* `sedes-backend-1` está corriendo en  **`http://localhost:8000/`** .
* `sedes-db-1` (PostGIS) está en el puerto  **`5432`** .
